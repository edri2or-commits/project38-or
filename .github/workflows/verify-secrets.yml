name: Verify Secret Access

on:
  workflow_dispatch:  # הרצה ידנית
  push:
    branches:
      - main
      - 'claude/**'

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SERVICE_ACCOUNT_KEY }}

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Verify Secret Manager Access
        run: |
          echo "🔐 Verifying Secret Manager access..."
          python src/secrets_manager.py

      - name: Test Secret Manager Module
        run: |
          python << 'EOF'
          import sys
          sys.path.insert(0, 'src')

          from secrets_manager import SecretManager

          print("\n🧪 Testing Secret Manager Module")
          print("=" * 50)

          manager = SecretManager()

          # List available secrets
          secrets = manager.list_secrets()
          print(f"\n✅ Found {len(secrets)} secret(s)")

          # Verify access without exposing values
          accessible_count = 0
          for secret in secrets:
              if manager.verify_access(secret):
                  accessible_count += 1

          print(f"✅ Can access {accessible_count} secret(s)")

          if accessible_count > 0:
              print("\n✨ Secret Manager is fully operational!")
              print("   All secrets are accessible but remain secure (not exposed)")
          else:
              print("\n⚠️  No secrets are accessible")
              print("   Check service account permissions")
              sys.exit(1)
          EOF

      - name: Summary
        run: |
          echo "✅ Secret Manager verification complete"
          echo "🔒 All secrets remain secure (never exposed in logs)"
          echo "🚀 System ready for autonomous secret access"
