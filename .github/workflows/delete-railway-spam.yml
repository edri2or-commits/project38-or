name: Delete Railway Spam Emails

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run - count but do not delete'
        required: false
        type: boolean
        default: false

permissions:
  contents: read
  id-token: write

jobs:
  delete-spam:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install google-auth google-auth-oauthlib google-api-python-client

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: 'projects/979429709900/locations/global/workloadIdentityPools/github-pool/providers/github-provider'
          service_account: 'claude-code-agent@project38-483612.iam.gserviceaccount.com'

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Delete Railway Spam
        env:
          DRY_RUN: ${{ inputs.dry_run }}
        run: |
          python3 << 'PYTHON_SCRIPT'
          import os
          from google.oauth2.credentials import Credentials
          from googleapiclient.discovery import build
          import subprocess
          import json

          # Get OAuth credentials from GCP Secret Manager
          def get_secret(name):
              result = subprocess.run(
                  ['gcloud', 'secrets', 'versions', 'access', 'latest', f'--secret={name}', '--project=project38-483612'],
                  capture_output=True, text=True
              )
              return result.stdout.strip()

          client_id = get_secret('GOOGLE-OAUTH-CLIENT-ID')
          client_secret = get_secret('GOOGLE-OAUTH-CLIENT-SECRET')
          refresh_token = get_secret('GOOGLE-OAUTH-REFRESH-TOKEN')

          # Create credentials
          creds = Credentials(
              token=None,
              refresh_token=refresh_token,
              token_uri='https://oauth2.googleapis.com/token',
              client_id=client_id,
              client_secret=client_secret,
              scopes=['https://www.googleapis.com/auth/gmail.modify']
          )

          # Build Gmail service
          service = build('gmail', 'v1', credentials=creds)

          # Search for Railway build failure emails
          query = 'from:railway.app subject:"Build failed" OR subject:"failed for delightful-cat"'
          print(f"🔍 Searching for: {query}")

          dry_run = os.environ.get('DRY_RUN', 'false').lower() == 'true'

          all_messages = []
          page_token = None

          while True:
              results = service.users().messages().list(
                  userId='me',
                  q=query,
                  pageToken=page_token,
                  maxResults=500
              ).execute()

              messages = results.get('messages', [])
              all_messages.extend(messages)

              page_token = results.get('nextPageToken')
              if not page_token:
                  break

              print(f"  Found {len(all_messages)} so far...")

          print(f"\n📧 Found {len(all_messages)} Railway spam emails")

          if not all_messages:
              print("✅ No spam emails to delete!")
              exit(0)

          if dry_run:
              print(f"\n🔒 DRY RUN - Would delete {len(all_messages)} emails")
              # Show sample
              for msg in all_messages[:5]:
                  msg_data = service.users().messages().get(userId='me', id=msg['id'], format='metadata').execute()
                  subject = next((h['value'] for h in msg_data['payload']['headers'] if h['name'] == 'Subject'), 'No subject')
                  print(f"  - {subject[:80]}")
              if len(all_messages) > 5:
                  print(f"  ... and {len(all_messages) - 5} more")
              exit(0)

          # Move to trash one by one (safest method)
          print(f"\n🗑️ Moving {len(all_messages)} emails to trash...")

          trashed = 0
          failed = 0

          for msg in all_messages:
              try:
                  service.users().messages().trash(userId='me', id=msg['id']).execute()
                  trashed += 1
                  if trashed % 50 == 0:
                      print(f"  Progress: {trashed}/{len(all_messages)}")
              except Exception as e:
                  failed += 1
                  if failed <= 5:
                      print(f"  Failed to trash message: {e}")

          print(f"\n✅ Moved {trashed} emails to trash")
          if failed > 0:
              print(f"⚠️ Failed to trash {failed} emails")
          PYTHON_SCRIPT
