# Functions Framework (pinned for Python 3.12 compatibility)
functions-framework==3.5.0
flask>=2.3.0
httpx>=0.25.0

# GCP will be accessed via gcloud CLI (already installed in Cloud Functions)
# This avoids Python 3.12 compatibility issues with heavy client libraries
