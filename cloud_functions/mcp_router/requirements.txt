# MCP Router Dependencies
# Compatible with both Cloud Functions and Cloud Run
# Python 3.12 compatible - pinned for stable builds
# Updated: 2026-01-18

# Web Framework
flask>=2.0.0

# HTTP Client (async)
httpx>=0.25.0

# Google Cloud Secret Manager - lazy loaded
google-cloud-secret-manager>=2.18.0

# Force Python 3.12 compatible versions
protobuf>=4.21.6
grpcio>=1.60.0
google-api-core>=2.15.0

# Production server (for Cloud Run)
gunicorn>=21.0.0
