# תוכנית יישום: סוכן אימייל חכם v2.0

**תאריך**: 2026-01-23
**מקור**: סינתזה של 3 מחקרי Deep Research (מאומתים מול מקורות חיצוניים)

---

## סיכום מנהלים

המחקר המעמיק חשף שהגישה הנוכחית ("שלח הודעה יבשה לטלגרם") רחוקה מהסטנדרט של 2026. הארכיטקטורה החדשה מבוססת על:

1. **LangGraph** כמנוע תזמור (לא n8n, שהוא רק "מערכת עצבים")
2. **מחקר אקטיבי** עם Tavily API
3. **פרסונה עברישית** עם תמיכת RTL מלאה
4. **ניתוב מודלים חכם** - 60% חיסכון בעלויות
5. **הגנת פרטיות** עם Presidio

---

## טכנולוגיות מאומתות

### ✅ אומתו מול מקורות חיצוניים

| טכנולוגיה | מה היא עושה | מקור |
|-----------|-------------|------|
| **LangGraph** | תזמור סוכנים עם `interrupt_before` | [LangChain Docs](https://docs.langchain.com/oss/python/langgraph/interrupts) |
| **Tavily** | חיפוש AI, 800K מפתחים, $25M גיוס | [Tavily Docs](https://docs.tavily.com/) |
| **Presidio** | זיהוי PII של מייקרוסופט | [GitHub](https://github.com/microsoft/presidio) |
| **WeasyPrint** | HTML ל-PDF, רישיון BSD | [WeasyPrint.org](https://weasyprint.org/) |
| **DictaLM 2.0** | LLM עברי, HuggingFace | [HuggingFace](https://huggingface.co/dicta-il) |
| **GLiNER** | NER zero-shot מהיר | [GitHub](https://github.com/urchade/GLiNER) |
| **Mem0** | שכבת זיכרון לסוכנים | [Mem0.ai](https://mem0.ai) |
| **Unicode RLM** | תו U+200F לערבוב RTL/LTR | [W3C](https://www.w3.org/TR/WCAG20-TECHS/H34.html) |

### ⚠️ דורשים בדיקה נוספת

| טכנולוגיה | סיבה |
|-----------|------|
| Antigravity | לא נמצא documentation ברור |
| Browserbase | עדיין בטא לפי המחקר |

---

## עקרונות מפתח מהמחקר

### 1. n8n = מערכת עצבים, לא מוח 🧠

```
❌ שגוי: n8n מפעיל לוגיקה מורכבת
✅ נכון: n8n מטריגר, Python חושב
```

n8n מצוין ל:
- טריגרים מתוזמנים (cron)
- webhooks
- התראות

n8n לא מתאים ל:
- תזמור LLM
- לוגיקה מורכבת
- ניהול state

### 2. Hebrish - עברית + אנגלית טבעית 🇮🇱

```
"היי! 👋 סרקתי את ה-inbox שלך וזיהיתי כמה emails שדורשים attention..."
```

זה הסגנון הטבעי של ישראלים בטק. לא עברית מלאכותית-פורמלית.

### 3. RTL Sandwich 🥪

```python
RLM = "\u200F"
text = f"{RLM}היי! בדקתי את ה-{RLM}API{RLM} ויש עדכון{RLM}"
```

עוטפים מילים באנגלית ב-RLM כדי שהתצוגה תהיה נכונה בטלגרם.

### 4. ניתוב מודלים - 60% חיסכון 💰

| משימה | מודל | עלות |
|-------|------|------|
| סיווג P1-P4 | Haiku | $0.25/MTok |
| יצירת הודעה | Sonnet | $3/MTok |
| ניתוח מורכב | Opus | $15/MTok (רק P1) |
| עברית | DictaLM | חינם (local) |

### 5. מחקר אקטיבי - מה שמבדיל סוכן חכם 🔍

```
הסוכן מקבל אימייל על "הזמנה ל-demo של Acme Corp"

❌ סוכן רגיל: "יש לך אימייל מ-Acme Corp"
✅ סוכן חכם:
   1. מחפש "Acme Corp" בגוגל
   2. קורא את האתר שלהם
   3. בודק אם יש reviews
   4. מדווח: "Acme Corp היא סטארטאפ ב-AI, גייסו $10M, המנכ"ל הוא..."
```

---

## ארכיטקטורה - תרשים זרימה

```
     ┌──────────────────────────────────────────────────────────────┐
     │                     SMART EMAIL AGENT v2.0                    │
     └──────────────────────────────────────────────────────────────┘
                                    │
     ╔══════════════════════════════╧═══════════════════════════════╗
     ║  TRIGGER: n8n cron (06:00 Israel) → Python Script            ║
     ╚══════════════════════════════╤═══════════════════════════════╝
                                    │
     ┌──────────────────────────────▼───────────────────────────────┐
     │  NODE 1: FETCH                                                │
     │  └─ Gmail API (existing gmail_client.py)                     │
     │  └─ Filter: last 24h, unread, skip system                    │
     └──────────────────────────────┬───────────────────────────────┘
                                    │
     ┌──────────────────────────────▼───────────────────────────────┐
     │  NODE 2: CLASSIFY (LLM: Haiku)                               │
     │  └─ P1 (Urgent) → Full research pipeline                     │
     │  └─ P2 (Important) → Light research                          │
     │  └─ P3 (Info) → Summary only                                 │
     │  └─ P4 (Low/System) → Skip or batch                          │
     └──────────────────────────────┬───────────────────────────────┘
                                    │
                    ┌───────────────┼───────────────┐
                    │ P1/P2         │ P3            │ P4
                    ▼               ▼               ▼
     ┌──────────────────────┐ ┌──────────┐ ┌──────────────┐
     │  NODE 3: RESEARCH    │ │  SKIP    │ │  BATCH       │
     │  └─ Tavily search    │ │  research│ │  SUMMARY     │
     │  └─ URL extraction   │ └────┬─────┘ └──────┬───────┘
     │  └─ Sender lookup    │      │              │
     │  └─ Website scrape   │      │              │
     └──────────┬───────────┘      │              │
                │                  │              │
                └────────┬─────────┘              │
                         │                        │
     ┌───────────────────▼────────────────────────▼─────────────────┐
     │  NODE 4: PII REDACT                                           │
     │  └─ Presidio analyzer                                         │
     │  └─ GLiNER for Hebrew entities                               │
     │  └─ Output: clean text + original (encrypted)                │
     └──────────────────────────────┬───────────────────────────────┘
                                    │
     ┌──────────────────────────────▼───────────────────────────────┐
     │  NODE 5: GENERATE MESSAGE (LLM: Sonnet)                      │
     │  └─ Hebrish persona                                          │
     │  └─ Include investigation report                             │
     │  └─ RTL formatting with RLM                                  │
     │  └─ Emojis for visual hierarchy                              │
     └──────────────────────────────┬───────────────────────────────┘
                                    │
     ┌──────────────────────────────▼───────────────────────────────┐
     │  NODE 6: SEND                                                 │
     │  └─ Telegram API (existing)                                  │
     │  └─ Optional: PDF report via WeasyPrint                      │
     │  └─ Log to memory (Mem0/SQLite)                              │
     └──────────────────────────────────────────────────────────────┘
```

---

## פאזות יישום

### Phase 1: MVP (שבוע 1) 🚀

```
[ ] יצירת מבנה LangGraph בסיסי
[ ] צומת סיווג עם Haiku
[ ] פורמט Hebrish בסיסי
[ ] אינטגרציה עם gmail_client קיים
```

**Deliverable**: סוכן שמסווג ומדבר בעברישית

### Phase 2: Intelligence (שבוע 2) 🧠

```
[ ] אינטגרציית Tavily למחקר
[ ] הגנת PII עם Presidio
[ ] היסטוריית שולחים (SQLite)
[ ] הרחבת פרסונה עברישית
```

**Deliverable**: סוכן שחוקר ומגן על פרטיות

### Phase 3: Polish (שבוע 3) ✨

```
[ ] דוחות PDF עם WeasyPrint
[ ] זיכרון עם Mem0
[ ] שקיפות חקירה
[ ] תבניות ויזואליות
```

**Deliverable**: סוכן עם דוחות יפים וזיכרון

### Phase 4: Production (שבוע 4) 🏭

```
[ ] deployment דרך n8n triggers
[ ] monitoring ומטריקות
[ ] feedback loop מהמשתמש
[ ] אופטימיזציית עלויות
```

**Deliverable**: סוכן production-ready

---

## מבנה קבצים

```
src/agents/
├── gmail_client.py              # ✅ קיים - נשאר
├── email_agent.py               # ← Refactor לאורכסטרטור
└── smart_email/
    ├── __init__.py
    ├── graph.py                 # LangGraph state machine
    ├── state.py                 # TypedDict לstate
    ├── nodes/
    │   ├── __init__.py
    │   ├── classify.py          # סיווג P1-P4
    │   ├── research.py          # מחקר אקטיבי
    │   ├── generate.py          # יצירת הודעה
    │   ├── pii_redact.py        # הגנת PII
    │   └── format_rtl.py        # פורמט RTL
    ├── memory.py                # Mem0/SQLite
    ├── persona.py               # פרומפטים עברישיים
    └── report.py                # WeasyPrint PDF
```

---

## דוגמת קוד: LangGraph Skeleton

```python
from langgraph.graph import StateGraph, END
from typing import TypedDict, List, Literal

class EmailState(TypedDict):
    emails: List[dict]
    classified: List[dict]
    research_results: dict
    message: str
    pii_clean: bool

def classify_emails(state: EmailState) -> EmailState:
    """Classify emails P1-P4 using Haiku"""
    # TODO: Implement with LiteLLM → Haiku
    pass

def research_priority(state: EmailState) -> EmailState:
    """Research P1/P2 emails with Tavily"""
    # TODO: Implement with Tavily API
    pass

def generate_message(state: EmailState) -> EmailState:
    """Generate Hebrish message with Sonnet"""
    # TODO: Implement with LiteLLM → Sonnet
    pass

def should_research(state: EmailState) -> Literal["research", "skip"]:
    """Route based on priority"""
    has_urgent = any(e["priority"] in ["P1", "P2"] for e in state["classified"])
    return "research" if has_urgent else "skip"

# Build graph
workflow = StateGraph(EmailState)

workflow.add_node("classify", classify_emails)
workflow.add_node("research", research_priority)
workflow.add_node("generate", generate_message)

workflow.set_entry_point("classify")
workflow.add_conditional_edges(
    "classify",
    should_research,
    {"research": "research", "skip": "generate"}
)
workflow.add_edge("research", "generate")
workflow.add_edge("generate", END)

app = workflow.compile()
```

---

## הערכת עלויות

### לפני (Opus בלבד)
- 50 אימיילים/יום × $0.50/אימייל = **$25/יום = $750/חודש**

### אחרי (ניתוב חכם)
| שלב | מודל | עלות/אימייל |
|-----|------|-------------|
| סיווג | Haiku | $0.001 |
| מחקר (20%) | Tavily | $0.008 |
| יצירה | Sonnet | $0.015 |
| **סה"כ** | | **$0.024** |

- 50 אימיילים/יום × $0.024 = **$1.20/יום = $36/חודש**
- **חיסכון: 95%** 🎉

---

## מה הלאה?

1. **אישור ADR-014** - ה-ADR כבר נכתב ב-`docs/decisions/ADR-014-smart-email-agent-architecture.md`
2. **התחלת Phase 1** - יצירת skeleton של LangGraph
3. **בחירת MVP scope** - מה הכי חשוב לך לראות קודם?

---

## מקורות (מאומתים)

- [LangGraph Docs](https://docs.langchain.com/oss/python/langgraph/)
- [Tavily API Docs](https://docs.tavily.com/)
- [Microsoft Presidio](https://github.com/microsoft/presidio)
- [WeasyPrint](https://weasyprint.org/)
- [W3C RTL Marks](https://www.w3.org/TR/WCAG20-TECHS/H34.html)
- [Mem0](https://mem0.ai/)
- [GLiNER](https://github.com/urchade/GLiNER)
- [DictaLM HuggingFace](https://huggingface.co/dicta-il)
